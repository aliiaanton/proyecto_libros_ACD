package com.bookmatch.backend.enums;

/**
 * Enumeración que define los roles de usuario en el sistema BookMatch.
 */
public enum Role {
    /** Usuario regular con permisos estándar */
    USER,

    /** Administrador con permisos elevados */
    ADMIN
}